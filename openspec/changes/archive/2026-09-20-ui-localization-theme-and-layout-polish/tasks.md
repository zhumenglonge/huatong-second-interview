# Tasks

## 1. UI 偏好与国际化基础

- [x] 1.1 建立中文/英文文案字典与客户端偏好状态，支持 localStorage 持久化，并验证刷新页面后语言选择仍被恢复
- [x] 1.2 在工作台顶部增加语言切换控件，接入主要导航、布局入口、任务总览、输入区和侧栏文案，并验证切换后主要视图无意中英混用
- [x] 1.3 为语言控件补充稳定的 aria-label、title 和当前动作语义，并通过组件测试或可访问性树验证

## 2. 浅色与深色主题

- [x] 2.1 建立浅色/深色主题 CSS 变量，覆盖页面背景、面板、边框、文字、输入框、弹层和状态反馈，并验证两种主题下核心文字对比度可读
- [x] 2.2 增加主题切换控件与持久化恢复逻辑，并验证会话流、任务画布和右侧面板可即时切换主题
- [x] 2.3 检查并替换核心工作区中遗留的硬编码浅色背景，验证深色主题下无明显白底、低对比或不可读控件

## 3. 右侧布局与文案清理

- [x] 3.1 将待办、结果、计算、笔记设置为默认显示且内容默认展开，并验证首次打开工作区时四个模块均可见
- [x] 3.2 保留模块独立开关、内容折叠和面板宽度记忆，验证关闭最后一个模块后面板收起、重新打开后状态正确恢复
- [x] 3.3 移除 `real QoderCN Agent backend`，统一主要用户可见静态文案语言，并通过全局检索确认不再出现该冗余文案

## 4. 集成验证

- [x] 4.1 更新或新增相关组件测试，覆盖语言切换、主题切换、默认右侧面板和布局开关行为，并验证测试套件通过
- [x] 4.2 运行 `npm run typecheck`、`npm test -- --run` 和 `npm run build`，并在中英文与浅色/深色组合下手动检查会话流、任务画布、输入区和右侧面板无回归
