# 实施任务

## 1. 项目数据模型与迁移

- [x] 1.1 增加项目持久化并初始化默认“快速任务”项目；验证新数据库包含默认项目。
- [x] 1.2 增加任务与项目的归属关系，并为旧任务编写幂等迁移；验证旧任务被分配到“快速任务”且数据没有丢失。
- [x] 1.3 扩展共享的项目/任务类型和项目列表查询；验证类型检查通过且项目记录可正确读写。

## 2. 项目范围内的 API 行为

- [x] 2.1 更新任务列表和创建接口，使其读取/使用项目上下文；验证不会返回其他项目的任务，且新任务绑定当前项目。
- [x] 2.2 审查任务详情、消息、取消、重试、批准和流式接口，保留任务归属并拒绝不可用的任务/项目；验证不会暴露跨项目数据。
- [x] 2.3 确认后台运行恢复仍然可以处理全部任务，而界面任务读取按项目隔离；验证现有任务执行和 SSE 行为正常。

## 3. 客户端工作区上下文

- [x] 3.1 在任务状态管理中加入项目列表和 `activeProjectId`，并提供项目选择动作；验证切换项目会关闭旧流并清理过期任务详情。
- [x] 3.2 让任务刷新、新建任务和任务操作使用当前项目；验证切换项目后只重新加载该项目的任务。
- [x] 3.3 安全处理空项目和不可用项目；验证空项目不会继续显示旧项目的选中任务或事件。

## 4. 侧栏与项目设置对齐

- [x] 4.1 重构项目顶部，使项目选择器、项目设置和折叠控件按线上顺序显示；验证无障碍名称和键盘操作。
- [x] 4.2 增加当前项目的设置界面，支持重命名并验证名称实时更新且不会改变任务选择。
- [x] 4.3 增加受保护的项目删除流程：二次确认、禁止删除非空项目、禁止删除默认“快速任务”项目，并在成功删除后切换到有效项目；验证各分支提示和数据状态。
- [x] 4.4 对齐侧栏折叠/展开、任务区/云盘区折叠的展示、间距、文案和响应式行为；验证支持的宽度下控件仍可用。

## 5. 验证

- [x] 5.1 增加数据库/API 测试，覆盖项目隔离、默认迁移、任务创建和跨项目访问拒绝；验证新增测试通过。
- [x] 5.2 增加状态管理和 Sidebar UI 测试，覆盖项目切换、流清理、项目设置和折叠控件；验证无障碍标签与界面状态正确。
- [x] 5.3 运行 `npm run typecheck`、`npm test` 和 `npm run build`；验证所有命令成功完成。
