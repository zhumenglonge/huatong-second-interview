# 任务清单

## 1. 主视图和任务总览基础

- [x] 1.1 在页面级视图模型中增加总览模式，并验证现有会话流和任务画布仍能正常渲染
- [x] 1.2 新增项目范围任务总览组件，使用现有 `TaskRow[]` 数据显示名称、状态、最近活动和创建时间；用测试数据验证任务行和空状态
- [x] 1.3 将总览任务行选择接入现有项目感知任务选择逻辑，并返回会话流；验证选择任务后能恢复任务会话且不改变当前项目

## 2. 任务总览交互

- [x] 2.1 增加总览文本搜索和清空控件；验证只保留标题或输入文本匹配的当前项目任务
- [x] 2.2 使用现有任务状态枚举增加“All statuses”和各状态筛选；验证搜索与状态筛选组合及无结果状态
- [x] 2.3 增加符合固定主区域布局的总览样式和溢出处理；验证长标题和时间字段下表格仍可使用

## 3. 侧栏任务控件

- [x] 3.1 增加独立的 `tasksOpen` 状态，使任务标题箭头隐藏/显示整个任务内容；验证两个任务分组一起消失，重新打开后保留子分组状态
- [x] 3.2 将新建任务移动到独立且有标签的控件；验证新建任务不会切换任务区域折叠状态
- [x] 3.3 实现侧栏搜索的打开、查询、清空和关闭，并按任务标题/输入文本过滤；验证无结果、Escape/清空行为以及折叠状态保持不变
- [x] 3.4 将“查看全部”连接到任务总览视图；验证当前项目不变且显示完整任务列表
- [x] 3.5 增加或更新 Sidebar 组件测试，覆盖控件标签、折叠语义、搜索过滤和总览回调行为

## 4. 集成与回归验证

- [x] 4.1 验证项目切换后侧栏和总览都刷新到新项目，且不会显示旧项目任务
- [x] 4.2 验证从侧栏或总览选择任务后，现有快照加载、SSE 重连、重试、取消和后台执行行为不受影响
- [x] 4.3 运行相关 Vitest 测试和生产 TypeScript/构建检查，并在交付前解决回归问题
