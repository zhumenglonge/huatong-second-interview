# 任务清单

## 1. Rail-logo 交互

- [x] 1.1 将装饰性的 rail-logo 元素替换为原生 button，接收 `onNewChat` 回调，提供明确的无障碍名称和提示文本，并验证其可以被聚焦和通过键盘激活。
- [x] 1.2 更新 rail-logo CSS，重置原生按钮样式，同时保留当前视觉外观、指针反馈和悬停行为，并增加清晰的焦点指示；在应用中进行视觉验证。

## 2. 空白会话状态切换

- [x] 2.1 在页面 shell 中将 Logo 回调连接到现有任务 store 的 `newTask()` action，并将页面视图设置为 `conv`；验证从已选会话激活 Logo 后会清理任务相关客户端状态，并使输入框保持可用。
- [x] 2.2 验证任务画布可见时激活 Logo 会返回会话流，而不会删除或取消当前及后台任务；确认原任务仍可从侧栏重新打开。

## 3. 验证

- [x] 3.1 新增或更新交互测试，覆盖鼠标激活、键盘激活、视图切换、选中状态清理和任务历史保留；验证相关测试命令通过。
- [x] 3.2 运行 `npm run typecheck` 和相关测试套件，然后在桌面布局下手动确认空白会话流程正常，且侧栏、输入框和画布导航没有回归。
